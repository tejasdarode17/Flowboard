
// !originalRequest._retry &&  //if _retry not exist.  current req me ham first time execution pr _retry add kar denge uske bad wohi req dobara ati hai to catch block chalega.  prevent infinite loop
//     !originalRequest.url?.includes("/auth/refresh") //only other req like /workspace should call the /auth/refresh if they fail.  /auth/refresh wali req nahi ani chahiye if block me prevent infinite loop

// originalRequest._retry = true  //we will add this so that after failing this current req that req should not come again. prevernt infinite loop


// failedQueue = [
//     {
//         resolve: ƒ resolveB(),
//         reject: ƒ rejectB()
//     },
//     {
//         resolve: ƒ resolveC(),
//         reject: ƒ rejectC()
//     }
// ]


// then is attached to the promise object


